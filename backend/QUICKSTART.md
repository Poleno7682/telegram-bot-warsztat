# Быстрый старт

## Локальная разработка (Windows/Linux/Mac)

### 1. Предварительные требования

- Python 3.11+
- Git
- Telegram Bot Token (получить у @BotFather)

### 2. Клонирование и установка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd Telegram-Bot-Warsztat/backend

# Установить зависимости (Linux/Mac - без venv)
python3 -m pip install --upgrade pip --user
python3 -m pip install -r requirements.txt --user
```

### 3. Настройка

```bash
# Копировать пример конфигурации
cp env.example .env

# Редактировать .env (указать BOT_TOKEN и ADMIN_IDS)
# Windows:
notepad .env
# Linux/Mac:
nano .env
```

Минимальная конфигурация `.env`:
```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
ADMIN_IDS=123456789
DATABASE_URL=sqlite+aiosqlite:///./db/bot.db
```

### 4. Инициализация базы данных

```bash
# Создать директорию для БД
mkdir db

# Запустить миграции
alembic upgrade head
```

### 5. Запуск бота

```bash
python -m app.main
```

Бот запущен! Теперь можно найти его в Telegram и отправить `/start`

## Первоначальная настройка в боте

### 1. Авторизация

После запуска `/start`, бот проверит ваш Telegram ID:
- Если ID в списке `ADMIN_IDS` - вы администратор
- Если ID в списке `MECHANIC_IDS` - вы механик
- Если ID в списке `USER_IDS` - вы пользователь

### 2. Выбор языка

При первом запуске выберите язык:
- 🇵🇱 Polski
- 🇷🇺 Русский

### 3. Добавление услуг (Администратор/Механик)

Прежде чем создавать записи, нужно добавить услуги:

1. Перейти в меню → Управление услугами
2. Добавить услугу
3. Указать:
   - Название на польском
   - Название на русском
   - Длительность (в минутах)
   - Цену (опционально)

Пример услуг:
- Wymiana oleju / Замена масла (60 мин)
- Wymiana opon / Замена шин (45 мин)
- Diagnostyka / Диагностика (30 мин)

### 4. Настройка рабочих часов (опционально)

По умолчанию:
- Рабочие часы: 08:00 - 16:00
- Шаг времени: 10 минут
- Буферное время: 15 минут

Можно изменить в меню → Настройки сервиса

### 5. Добавление механиков и пользователей

Администратор может добавить новых пользователей через бота:

1. Меню администратора → Управление пользователями
2. Выбрать роль (Механик или Пользователь)
3. Ввести Telegram ID пользователя

**Как узнать Telegram ID:**
- Использовать бота @userinfobot
- Или попросить пользователя отправить сообщение вашему боту и посмотреть ID в логах

## Workflow использования

### Создание записи (Пользователь)

1. Главное меню → Новая запись
2. Выбрать услугу
3. Выбрать дату
4. Выбрать время (бот показывает только свободные слоты)
5. Ввести данные автомобиля:
   - Марка
   - Модель
   - Номер
6. Ввести данные клиента:
   - Имя
   - Телефон
7. Ввести описание проблемы

Бот автоматически переведет описание на оба языка и отправит уведомление всем механикам.

### Обработка записи (Механик)

Механик получает уведомление с кнопками:
- ✅ Принять
- ❌ Отклонить
- ⏰ Изменить время

#### Принять запись
- Запись подтверждается
- Все получают уведомление
- Запись сохраняется в БД

#### Отклонить запись
- Запись отклоняется
- Все получают уведомление
- Запись НЕ сохраняется

#### Изменить время
1. Механик выбирает новую дату и время
2. Пользователь получает предложение с кнопками:
   - Подтвердить
   - Предложить другое время
3. Процесс продолжается до согласования
4. После согласования запись сохраняется

## Тестирование функционала

### Тест 1: Создание записи

```
Пользователь:
1. /start
2. Выбрать язык
3. Новая запись
4. Выбрать "Wymiana oleju"
5. Выбрать завтрашний день
6. Выбрать время 10:00
7. Ввести: Toyota
8. Ввести: Corolla
9. Ввести: WAW12345
10. Ввести: Jan Kowalski
11. Ввести: +48123456789
12. Ввести: Wymiana oleju i filtrów

Ожидаемый результат:
- Создана запись
- Механики получили уведомление
```

### Тест 2: Принятие записи

```
Механик:
1. Получить уведомление
2. Нажать ✅ Принять

Ожидаемый результат:
- Запись принята
- Пользователь получил подтверждение
- Остальные механики получили уведомление
```

### Тест 3: Изменение времени

```
Механик:
1. Получить уведомление
2. Нажать ⏰ Изменить время
3. Выбрать новую дату
4. Выбрать новое время

Пользователь:
5. Получить предложение
6. Нажать Подтвердить

Ожидаемый результат:
- Время изменено
- Все получили уведомления
- Запись сохранена с новым временем
```

### Тест 4: Проверка свободных слотов

```
1. Создать запись на 10:00 (60 мин услуга)
2. Попробовать создать еще одну запись на тот же день

Ожидаемый результат:
- Время 10:00-11:15 (60 мин + 15 буфер) недоступно
- Доступны слоты до 10:00 и после 11:15
```

## Структура проекта

```
backend/
├── app/
│   ├── bot/               # Telegram bot logic
│   │   ├── handlers/      # Message handlers
│   │   ├── keyboards/     # Inline keyboards
│   │   ├── middlewares/   # Auth, i18n, DB middlewares
│   │   └── states/        # FSM states
│   ├── config/            # Configuration
│   ├── core/              # Core utilities (i18n)
│   ├── models/            # SQLAlchemy models
│   ├── repositories/      # Repository pattern
│   ├── services/          # Business logic
│   └── main.py           # Entry point
├── alembic/              # Database migrations
├── scripts/              # Deployment scripts
└── systemd/              # Systemd service
```

## SOLID принципы в проекте

### Single Responsibility Principle (SRP)
- Каждый сервис отвечает за одну функцию
- `AuthService` - только авторизация
- `BookingService` - только бронирования
- `TimeService` - только расчет времени

### Open/Closed Principle (OCP)
- Легко добавить новый язык (просто добавить JSON файл)
- Легко добавить новую роль пользователя

### Liskov Substitution Principle (LSP)
- BaseRepository может быть заменен любым конкретным репозиторием

### Interface Segregation Principle (ISP)
- Middleware-ы разделены по функциям
- Репозитории имеют специфичные методы

### Dependency Inversion Principle (DIP)
- Handlers зависят от сервисов, а не от деталей реализации
- Сервисы зависят от репозиториев (абстракций)

## Troubleshooting

### Ошибка: "Bot token is invalid"
- Проверьте BOT_TOKEN в .env
- Получите новый token у @BotFather

### Ошибка: "User not authorized"
- Добавьте свой Telegram ID в ADMIN_IDS в .env
- Узнать ID: @userinfobot

### Ошибка: "Database not found"
- Создайте директорию: `mkdir db`
- Запустите миграции: `alembic upgrade head`

### Бот не отвечает
- Проверьте что бот запущен: `python -m app.main`
- Проверьте логи в консоли и bot.log

## Следующие шаги

1. Добавьте реальных пользователей через бота
2. Настройте рабочие часы под ваш график
3. Добавьте все необходимые услуги
4. Протестируйте workflow создания и принятия записей
5. При готовности к production - разверните на VPS:

```bash
# Установить зависимости
sudo bash scripts/setup.sh

# Установить как systemd service
sudo bash scripts/install_service.sh
```

Подробнее: [DEPLOYMENT.md](DEPLOYMENT.md)

